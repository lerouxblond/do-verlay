# internal/handlers (différé)

Couche HTTP : décode les requêtes, appelle les **services**, encode les réponses. **Aucune logique
métier, aucun SQL ici.**

TODO : `profil`, `module`, `dofusdex`, `guilde`, `personnage`, `message` (CRUD REST) + endpoint
WebSocket de synchro par profil.
