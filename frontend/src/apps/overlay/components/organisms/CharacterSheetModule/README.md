# CharacterSheetModule (organism · overlay)

Fiche personnage : médaillon (masque + fondu), nom, classe + serveur, niveau et points de succès.

| Prop    | Type        | Rôle                  |
| ------- | ----------- | --------------------- |
| `perso` | `Character` | Données du personnage |

Illustration de classe résolue via `classCharacter(classe, genre)`. Dépendances : `CardShell`,
`Avatar`.
